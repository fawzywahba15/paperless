spring:
  datasource:
    url: jdbc:tc:postgresql:16:///paperless?TC_TMPFS=/testtmpfs:rw
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  rabbitmq:
    host: localhost   # wird per DynamicPropertySource überschrieben
    port: 5672
    username: guest
    password: guest

paperless:
  minio:
    bucket: documents
    accessKey: testkey
    secretKey: testsecret
    secure: false
    endpoint: http://localhost:9000  # überschreiben wir dynamisch im Test
